import numpy as  np


X = np.array([[1],[2],[3],[4],[5]], dtype=float)
y = np.array([1.5, 3.0, 4.5, 6.0, 7.5], dtype=float)

def batch_gradient_descent(X, y, w, b, alpha, iterations):


def stochastic_gradient_descent(X, y, w, b, alpha, iterations):



def mini_batch_gradient_descent(X, y, w, b, alpha, iterations, batch_size=32):

